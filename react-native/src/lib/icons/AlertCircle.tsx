import { AlertCircle } from 'lucide-react-native';
import { iconWithClassName } from './iconWithClassName';

const StyledAlertCircle = iconWithClassName(AlertCircle);
export { StyledAlertCircle as AlertCircle };
