{
  "name": "@nayan-ui/root",
  "version": "1.0.0",
  "private": true,
  "description": "Reusable Component Libraries for React and React Native.",
  "license": "ISC",
  "workspaces": [
    "packages/*",
    "packages/native/example",
    "applications/*"
  ],
  "scripts": {
    "format": "oxfmt",
    "clean": "rimraf packages/*/node_modules packages/*/dist packages/*/lib node_modules",
    "ai:build": "bun run --filter @nayan-ui/ai build",
    "cli:build": "bun run --filter @nayan-ui/cli build",
    "react:dev": "bun run --filter @nayan-ui/react dev",
    "react:build": "bun run --filter @nayan-ui/react build",
    "native:build": "bun run --filter @nayan-ui/native build",
    "native:typecheck": "bun run --filter @nayan-ui/native typecheck",
    "native:clean": "bun run --filter @nayan-ui/native clean",
    "native:example": "bun run --filter @nayan-ui/native example",
    "website:dev": "bun run react:build && bun run --filter @nayan-ui/website dev",
    "website:build": "bun run react:build && bun run editor:build && bun run --filter @nayan-ui/website build",
    "website:start": "bun run --filter @nayan-ui/website start",
    "editor:dev": "bun run --filter @nayan-ui/video-editor dev",
    "editor:build": "bun run --filter @nayan-ui/video-editor build"
  },
  "devDependencies": {
    "oxfmt": "^0.68.0",
    "rimraf": "^6.0.1"
  }
}
